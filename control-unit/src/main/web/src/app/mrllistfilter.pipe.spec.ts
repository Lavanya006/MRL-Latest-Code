import { MrllistfilterPipe } from './mrllistfilter.pipe';

describe('MrllistfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MrllistfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
